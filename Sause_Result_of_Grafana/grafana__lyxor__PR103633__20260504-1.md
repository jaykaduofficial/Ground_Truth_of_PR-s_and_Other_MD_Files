# PR Review: lyxor-pr-testing-org/grafana__grafana__lyxor__PR103633__20260504 #1

- **PR:** https://github.com/lyxor-pr-testing-org/grafana__grafana__lyxor__PR103633__20260504/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR103633__20260504@main`
- **PR scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR103633__20260504@pr:1`
- **Files changed:** 4
- **Route:** code_pr_ensemble
- **Reviewed:** 7/16/2026, 9:04:51 AM

## Metrics

- **Findings:** 4 unique (5 raw) · **Files flagged:** 4 · **Density:** 1 findings/file
- **Severity:** critical 0 · high 0 · medium 3 · info 1
- **Files changed:** 4
- **Route:** code_pr_ensemble
- **By category:** general 4
- **Top files:** rbac.go (1), cache.go (1), service_test.go (1), service.go (1)
- **Sources:** lens 0 · llm 5 · merged 4
- **Duplicates merged:** 1

## Summary

The new `permDenialCache` can short-circuit `Check()` to deny solely from a cached `(namespace,userUID,action…)`-derived key, which risks incorrect denials if the key or cache invalidation isn’t perfectly safe. The denial-cache key is built by concatenating raw fields with underscores, which can collide if components contain underscores/separators. Test coverage regressed slightly (a cached `getUserPermissions` assertion was removed), and `ProvideAuthZClient` now explicitly uses a `NoopCache`, potentially changing prior default caching behavior.

## Findings

### MEDIUM · general

- **Location:** `pkg/services/authz/rbac/service.go:111–172`
- **Lens:** llm
- **Rationale:** The new permDenialCache short-circuits Check() to deny purely based on a cached key derived from (namespace,userUID,action,name,parent). This risks incorrect denials if inputs are not canonicalized (e.g., empty vs root parent folder, name normalization), if permissions change within TTL, or if the action mapping differs between cached perms and check evaluation; it also bypasses any additional logic that might otherwise allow (e.g., future rule changes) until expiry.
- **Suggestion:** Ensure key components are canonicalized (normalize empty/"/" parent folder, name casing/escaping rules as applicable) and consider including identity type and/or group/resource/verb in the denial key (or ensure checkReq.Action fully captures those). Add targeted tests that permission changes within TTL do not cause prolonged false-deny (or document/accept staleness) and that different resources with same name/parent do not collide.

### MEDIUM · general

- **Location:** `pkg/services/authz/rbac/cache.go:27–35`
- **Lens:** llm
- **Rationale:** userPermDenialCacheKey concatenates raw fields with underscores. If any component can contain underscores or other separators, different tuples could collide, leading to incorrect authorization decisions (false denies).
- **Suggestion:** Use a structured encoding for cache keys (e.g., URL-escape each component, or join with a delimiter after escaping, or hash a serialized tuple). Add a unit test covering collision-prone inputs (e.g., name/parent containing underscores) to prove uniqueness.

### MEDIUM · general

- **Location:** `pkg/services/authz/rbac/service_test.go:339–356`
- **Lens:** llm
- **Rationale:** A test case asserting getUserPermissions returns cached results was removed, and while new tests cover Check() cache allow and DB fallback, there is no direct test that getCachedIdentityPermissions hits the cache for anonymous/user identities or that denial caching actually short-circuits subsequent calls.
- **Suggestion:** Reintroduce/replace cache-hit tests covering: (1) anonymous cache hit path, (2) user/service-account cache hit path via idCache->permCache, (3) denial cache short-circuit (first request denies and sets cache; second request should not call store). Use spies/counters on fakeStore to assert no DB calls on cache hits.

### INFO · general

- **Location:** `pkg/services/authz/rbac.go:98–121`
- **Lens:** llm
- **Rationale:** ProvideAuthZClient now creates an authzlib client with an explicit NoopCache. This changes behavior from any previous default client caching behavior and may increase remote calls in some deployments, but it is likely intentional given the new server-side caching improvements.
- **Suggestion:** Confirm this is intended for the in-process client path and add a short comment explaining why client-side caching is disabled here (to avoid double caching / staleness). Consider a small integration test or benchmark to validate expected call reduction occurs server-side.

## Merged duplicates

### INFO · general (duplicate)

- **Location:** `pkg/services/authz/rbac.go:235–251`
- **Lens:** llm
- **Rationale:** NoopCache is exported and placed in rbac.go; it appears to be an internal implementation detail used only for client options. Keeping it exported may unintentionally expand API surface within the package.
- **Merged into:** `llm.rbac.go`
