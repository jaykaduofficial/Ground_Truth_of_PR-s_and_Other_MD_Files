# PR Review: lyxor-pr-testing-org/grafana__grafana__lyxor__PR107534__20260504 #1

- **PR:** https://github.com/lyxor-pr-testing-org/grafana__grafana__lyxor__PR107534__20260504/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR107534__20260504@main`
- **PR scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR107534__20260504@pr:1`
- **Files changed:** 4
- **Route:** code_pr_ensemble
- **Reviewed:** 7/16/2026, 9:10:01 AM

## Metrics

- **Findings:** 4 unique (5 raw) · **Files flagged:** 4 · **Density:** 1 findings/file
- **Severity:** critical 0 · high 1 · medium 3
- **Files changed:** 4
- **Route:** code_pr_ensemble
- **By category:** general 3 · authz 1
- **Top files:** querySplitting.test.ts (1), querySplitting.ts (1), shardQuerySplitting.test.ts (1), shardQuerySplitting.ts (1)
- **Sources:** lens 0 · llm 5 · merged 4
- **Duplicates merged:** 1

## Summary

The change shifts query interpolation from `interpolateVariablesInQueries(request.targets, request.scopedVars)` to per-target `datasource.applyTemplateVariables`, which may introduce behavior changes and potential mutation of the original query objects depending on the datasource implementation. Tests were updated to stub `applyTemplateVariables` (a coupling change) and currently only validate single-target interpolation, leaving gaps around multi-target scenarios and ensuring non-interpolated targets remain unaffected.

## Findings

### HIGH · authz

- **Location:** `public/app/plugins/datasource/loki/shardQuerySplitting.test.ts:53–114`
- **Lens:** llm
- **Rationale:** Tests were updated to stub `applyTemplateVariables` instead of `interpolateVariablesInQueries`, implying a functional contract change. If any production codepaths (or plugins/extensions) still rely on `interpolateVariablesInQueries` being invoked by shard splitting, this is a behavioral breaking change even if the method still exists on the datasource.
- **Suggestion:** If `interpolateVariablesInQueries` is deprecated, document it and/or keep backward-compatible invocation (e.g., call it when present, or implement it in terms of `applyTemplateVariables`). If not deprecated, consider keeping the old call or adding a compatibility shim. Add a changelog note if this affects downstream consumers.

### MEDIUM · general

- **Location:** `public/app/plugins/datasource/loki/querySplitting.ts:293–299`
- **Lens:** llm
- **Rationale:** Mapping `datasource.applyTemplateVariables` over targets may mutate the original query objects (depending on implementation). If it mutates in-place, later code or callers that reuse `request.targets` could see unexpected changes (e.g., cached requests, UI state, subsequent retries), which can be hard to diagnose.
- **Suggestion:** Ensure `applyTemplateVariables` returns a new query object (immutable) or defensively clone before applying (e.g., `.map(q => datasource.applyTemplateVariables({ ...q }, ...))`). Add a test asserting `request.targets` remains unchanged after `runSplitQuery`.

### MEDIUM · general

- **Location:** `public/app/plugins/datasource/loki/shardQuerySplitting.ts:46–52`
- **Lens:** llm
- **Rationale:** This switches from `interpolateVariablesInQueries(request.targets, request.scopedVars)` to per-query `applyTemplateVariables(query, request.scopedVars, request.filters)`. If `interpolateVariablesInQueries` previously applied additional Loki-specific logic (batch behavior, escaping differences, or scoped var handling), behavior could change subtly, especially for complex variable types or ad-hoc filters.
- **Suggestion:** Confirm `applyTemplateVariables` is semantically equivalent (or intended to be) to the removed interpolation call for shard splitting. Add coverage for multi-variable queries and ad-hoc filters (`request.filters`) to ensure identical final expressions vs the previous behavior.

### MEDIUM · general

- **Location:** `public/app/plugins/datasource/loki/querySplitting.test.ts:72–90`
- **Lens:** llm
- **Rationale:** The new test validates expr and step interpolation for a single target, but does not cover multiple targets where one is hidden/empty, or ensure interpolation occurs before partitioning/splitting decisions (e.g., variable expansion changes `isLogsQuery` or `querySupportsSplitting`).
- **Suggestion:** Add tests with multiple targets including hidden/empty expr, and a case where variable interpolation changes query type (logs vs metrics) or splitting support, asserting the correct branch is taken.

## Merged duplicates

### MEDIUM · general (duplicate)

- **Location:** `public/app/plugins/datasource/loki/shardQuerySplitting.test.ts:81–114`
- **Lens:** llm
- **Rationale:** The expectation `applyTemplateVariables` is called 5 times is coupled to internal shard grouping/counting behavior, which may make the test brittle and mask real regressions (a refactor could change call counts without changing correctness).
- **Merged into:** `llm.shardquerysplitting.test.ts`
