# PR Review: lyxor-pr-testing-org/grafana__grafana__lyxor__PR97529__20260504 #1

- **PR:** https://github.com/lyxor-pr-testing-org/grafana__grafana__lyxor__PR97529__20260504/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR97529__20260504@main`
- **PR scope:** `code:lyxor-pr-testing-org/grafana__grafana__lyxor__PR97529__20260504@pr:1`
- **Files changed:** 5
- **Route:** code_pr_ensemble
- **Reviewed:** 7/14/2026, 5:14:38 PM

## Metrics

- **Findings:** 5 unique (8 raw) · **Files flagged:** 5 · **Density:** 1 findings/file
- **Severity:** critical 0 · high 0 · medium 4 · info 1
- **Files changed:** 5
- **Route:** code_pr_ensemble
- **By category:** general 4 · authz 1
- **Top files:** module_server_test.go (1), search.go (1), server.go (1), bleve.go (1), backend.go (1)
- **Sources:** lens 0 · llm 8 · merged 5
- **Duplicates merged:** 3

## Summary

This PR improves tracing/context propagation and centralizes logging, but it also changes lifecycle semantics by calling `Init` during `NewResourceServer`, making construction fail on transient dependencies. Removing the global lock around Bleve `BuildIndex` introduces a potential race for concurrent builds of the same key, and the updated warning log lacks enough detail beyond latency. Skipping the Postgres integration test reduces coverage for a widely used backend and could hide regressions.

## Findings

### MEDIUM · authz

- **Location:** `pkg/storage/unified/resource/server.go:255–266`
- **Lens:** llm
- **Rationale:** Calling s.Init(ctx) inside NewResourceServer changes lifecycle semantics: construction can now fail due to transient dependencies (DB/search backend) and blocks returning a usable server. Callers that previously expected NewResourceServer to be lightweight and for Init to be deferred to first use may now see startup failures or different error timing.
- **Suggestion:** Verify and document that NewResourceServer is only used on startup paths where blocking init is acceptable; otherwise consider introducing an explicit Start/Init step in the module wiring instead of doing it in the constructor, or gate eager init behind an option/flag.

### MEDIUM · general

- **Location:** `pkg/storage/unified/search/bleve.go:85–149`
- **Lens:** llm
- **Rationale:** Removing the global cacheMu lock around BuildIndex allows concurrent BuildIndex calls for the same key to race, potentially building multiple indexes and overwriting b.cache[key], leaking resources and causing inconsistent metrics/handles. The final cache assignment is locked, but the expensive build path is now uncoordinated per key.
- **Suggestion:** Add per-key singleflight (golang.org/x/sync/singleflight) or a per-key mutex to ensure only one build occurs per key; alternatively, check cache under lock before building and re-check before storing, closing/discarding a duplicate index if another goroutine won.

### MEDIUM · general

- **Location:** `pkg/storage/unified/resource/search.go:277–283`
- **Lens:** llm
- **Rationale:** Switching from the CLI logger to s.log is good for consistent server logging, but the warning logs only latency without key/namespace context, which reduces debuggability for production incidents.
- **Suggestion:** Include identifying fields in the warning log (namespace/resource/group/name/tenant if available) and consider sampling/rate-limiting to avoid log spam during systemic slowness.

### MEDIUM · general

- **Location:** `pkg/server/module_server_test.go:32–42`
- **Lens:** llm
- **Rationale:** Skipping the integration test for Postgres reduces coverage on a commonly used DB backend and can mask regressions, especially since this PR changes initialization timing and startup behavior where DB differences matter.
- **Suggestion:** Fix the Drone/Postgres flake by stabilizing the test environment (timeouts, port collisions, migrations, or container readiness) or add a smaller deterministic unit/integration test that validates instrumentation server behavior without relying on Drone-specific Postgres behavior.

### INFO · general

- **Location:** `pkg/storage/unified/sql/backend.go:123–130`
- **Lens:** llm
- **Rationale:** Propagating the derived ctx from tracer.Start in GetResourceStats improves trace correlation for downstream SQL work.
- **Suggestion:** No change required; consider adding a small test or lint rule to catch unused ctx from tracer.Start across the codebase.

## Merged duplicates

### MEDIUM · general (duplicate)

- **Location:** `pkg/storage/unified/resource/server.go:446–1018`
- **Lens:** llm
- **Rationale:** Removing per-RPC s.Init(ctx) calls assumes Init is always executed successfully before any RPC handlers are invoked. If any caller can obtain a server instance without NewResourceServer (or if Init fails but the instance is still used), requests may operate on partially initialized fields (watcher/search/diagnostics/blob), leading to nil dereferences or silent misbehavior.
- **Merged into:** `llm.server.go`

### MEDIUM · general (duplicate)

- **Location:** `pkg/storage/unified/resource/server.go:294–318`
- **Lens:** llm
- **Rationale:** Reordering init to build the search index before starting the watcher can reduce missed events during index build, but it may also increase the window where writes occur without being watched (depending on how events are produced) and can delay watch availability. This is a correctness/perf tradeoff that can affect freshness guarantees.
- **Merged into:** `llm.server.go`

### MEDIUM · general (duplicate)

- **Location:** `pkg/storage/unified/resource/search.go:277–283`
- **Lens:** llm
- **Rationale:** Switching from the CLI logger to s.log is good for consistent server logging, but the warning logs only latency without key/namespace context, which reduces debuggability for production incidents.
- **Merged into:** `llm.search.go`
