# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR8087__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR8087__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR8087__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR8087__20260430@pr:1`
- **Files changed:** 12
- **Route:** code_pr_ensemble
- **Reviewed:** 7/15/2026, 8:37:16 PM

## Metrics

- **Findings:** 5 unique (9 raw) · **Files flagged:** 6 · **Density:** 0.5 findings/file
- **Severity:** critical 0 · high 3 · medium 3
- **Files changed:** 12
- **Route:** code_pr_ensemble
- **By category:** general 5 · authz 1
- **Top files:** getCalendar.ts (1), index.ts (1), reschedule.ts (1), reschedule.ts (1), CalendarManager.ts (1)
- **Sources:** lens 0 · llm 9 · merged 6
- **Duplicates merged:** 3

## Summary

This PR introduces breaking async behavior: `getCalendar` becomes async and `appStore` entries are now `import()` Promises, so callers expecting synchronous module/namespace values will break unless updated (including `CalendarManager` and `videoClient` consumers). It also contains a high-risk async control-flow bug in both `vital` and `wipemycalother` reschedule code where `forEach(async ...)` isn’t awaited, so calendar/video deletions may run after the parent function completes or not fully finish. Overall, ensure all downstream consumers are updated for the new Promise-based APIs and replace the `forEach` patterns with `for...of` + `await` or `Promise.all`.

## Findings

### HIGH · authz

- **Location:** `packages/app-store/_utils/getCalendar.ts:1–30`
- **Lens:** llm
- **Rationale:** `getCalendar` changes from synchronous to async, which is a breaking API change for any callers not updated (including external consumers of the package). Any missed call sites will now receive a Promise instead of a Calendar instance, causing runtime failures or silent no-ops (e.g., optional chaining on a Promise).
- **Suggestion:** Confirm all internal and external call sites are updated to `await getCalendar(...)`. If this is a public API, consider exporting a new async function name (e.g., `getCalendarAsync`) and keeping a sync wrapper only if feasible, or document the breaking change and bump major version.

### HIGH · general

- **Location:** `packages/app-store/vital/lib/reschedule.ts:110–160`
- **Lens:** llm
- **Rationale:** `bookingRefsFiltered.forEach(async ...)` does not await the async callbacks. Deletions can continue after the parent function completes, and exceptions inside the callback won't be caught by the surrounding try/catch, leading to inconsistent reschedule cleanup and unhandled promise rejections.
- **Suggestion:** Replace `forEach` with `await Promise.all(bookingRefsFiltered.map(async (bookingRef) => { ... }))` or use a `for...of` loop with `await` to ensure deletions complete and errors are handled deterministically.

### HIGH · general

- **Location:** `packages/app-store/wipemycalother/lib/reschedule.ts:110–160`
- **Lens:** llm
- **Rationale:** Same async-control-flow bug as in vital: `forEach(async ...)` is not awaited, so calendar/video deletions may not run to completion before reschedule finishes and errors may bypass the try/catch.
- **Suggestion:** Use `await Promise.all(...)` over a mapped array of promises or a `for...of` loop with `await`.

### MEDIUM · general

- **Location:** `packages/app-store/index.ts:1–40`
- **Lens:** llm
- **Rationale:** The appStore values are now Promises from `import()`, meaning `appStore[key]` is no longer the module namespace object. Any remaining code that does property checks without awaiting (e.g., `'lib' in app`) will behave incorrectly or throw, and runtime behavior now depends on async module resolution.
- **Suggestion:** Type `appStore` explicitly as `Record<string, Promise<any>>` (or better, typed module namespaces) and ensure all usage sites consistently `await appStore[name]` before inspecting `lib`/factories. Consider a helper `loadApp(appName)` that caches resolved modules and centralizes error handling.

### MEDIUM · general

- **Location:** `packages/core/CalendarManager.ts:1–80`
- **Lens:** llm
- **Rationale:** The code now treats `item.calendar` as awaitable (`const calendar = await item.calendar;`), implying earlier steps may be returning a Promise. If any element of `calendarCredentials` still contains a resolved Calendar (not a Promise), `await` is harmless, but mixed types indicate an unclear contract and increases the chance of subtle bugs.
- **Suggestion:** Normalize `calendarCredentials` to store either a `calendarPromise: Promise<Calendar|null>` or an already-resolved `calendar: Calendar|null` consistently. Update typing so `item.calendar` is explicitly `Promise<Calendar|null>` if that's the intended design.

### MEDIUM · general

- **Location:** `packages/core/videoClient.ts:18–90`
- **Lens:** llm
- **Rationale:** Refactor from `reduce` to `for...of` is clearer, but the new function returns a Promise; ensure all consumers are updated. Any missed caller will now map over a Promise and fail at runtime.
- **Suggestion:** Search for all usages of `getVideoAdapters` and enforce `await`. Consider renaming to `getVideoAdaptersAsync` to prevent accidental sync usage.

## Merged duplicates

### MEDIUM · general (duplicate)

- **Location:** `packages/app-store/_utils/getCalendar.ts:1–30`
- **Lens:** llm
- **Rationale:** This change fundamentally alters module loading and async behavior; regressions are likely around missing implementations, unknown calendar types, and handling of `_other_calendar` suffix normalization. There are no shown tests verifying the new async path and error behavior.
- **Merged into:** `llm.getcalendar.ts`

### MEDIUM · general (duplicate)

- **Location:** `packages/app-store/vital/lib/reschedule.ts:110–160`
- **Lens:** llm
- **Rationale:** Given the async refactor, reschedule cleanup is a high-risk flow (calendar event deletions + video meeting deletions). Lack of tests makes it easy to miss that deletions are not awaited or that failures aren't propagated.
- **Merged into:** `llm.reschedule.ts`

### MEDIUM · general (duplicate)

- **Location:** `packages/core/videoClient.ts:18–90`
- **Lens:** llm
- **Rationale:** Refactor from `reduce` to `for...of` is clearer, but the new function returns a Promise; ensure all consumers are updated. Any missed caller will now map over a Promise and fail at runtime.
- **Merged into:** `llm.videoclient.ts`
