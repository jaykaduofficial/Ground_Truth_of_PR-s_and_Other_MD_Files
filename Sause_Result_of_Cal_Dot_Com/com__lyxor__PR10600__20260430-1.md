# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR10600__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR10600__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR10600__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR10600__20260430@pr:1`
- **Files changed:** 16
- **Route:** code_pr_ensemble
- **Reviewed:** 7/15/2026, 8:11:50 PM

## Metrics

- **Findings:** 4 unique (8 raw) · **Files flagged:** 4 · **Density:** 0.3 findings/file
- **Severity:** critical 0 · high 0 · medium 3 · info 1
- **Files changed:** 16
- **Route:** code_pr_ensemble
- **By category:** general 2 · authz 1 · security 1
- **Top files:** BackupCode.tsx (1), TwoFactor.tsx (1), DisableTwoFactorModal.tsx (1), EnableTwoFactorModal.tsx (1)
- **Sources:** lens 0 · llm 8 · merged 4
- **Duplicates merged:** 4

## Summary

Review flags a couple of medium-severity maintainability/security issues: switching between TOTP and backup codes calls `resetForm(false)` which retains the password while clearing factor fields, and `backupCodesUrl` is created/stored via `URL.createObjectURL` but not used in the diff and only conditionally revoked (risking leaks). Also, `BackupCode` is misnamed as `default function TwoFactor`, hurting clarity, while the added `autoFocus` prop in `TwoFactor` is a nice composability improvement.

## Findings

### MEDIUM · authz

- **Location:** `apps/web/components/settings/DisableTwoFactorModal.tsx:70–130`
- **Lens:** llm
- **Rationale:** Toggling between TOTP and backup code calls resetForm(false), which keeps the password while clearing the factor fields. This is convenient but increases the chance of leaving a sensitive password in form state when switching views, especially if the modal stays open.
- **Suggestion:** Consider clearing the password when toggling modes (or make it configurable) and ensure password is cleared on modal close/unmount as well.

### MEDIUM · security

- **Location:** `apps/web/components/settings/EnableTwoFactorModal.tsx:60–280`
- **Lens:** llm
- **Rationale:** backupCodesUrl is created (Blob + URL.createObjectURL) and stored but not used in the shown diff, and it is only revoked when a new URL is created. If the modal is opened/closed multiple times, this can leak object URLs and keep backup codes in memory longer than necessary.
- **Suggestion:** Either remove backupCodesUrl if unused, or add a download link that uses it and revoke it in a cleanup effect and/or resetState (and on unmount/close).

### MEDIUM · general

- **Location:** `apps/web/components/auth/BackupCode.tsx:1–29`
- **Lens:** llm
- **Rationale:** The BackupCode component is exported as default function TwoFactor (misnamed), which reduces maintainability and can confuse debugging/stack traces. Also, minLength/maxLength rely on the placeholder format but do not enforce the dash position or digit-only content, potentially causing avoidable validation errors server-side.
- **Suggestion:** Rename the function to BackupCode to match the file/import. Add client-side validation/pattern (e.g., /^\d{5}-\d{5}$/ or allow digits-only and normalize) and normalize before submit.

### INFO · general

- **Location:** `apps/web/components/auth/TwoFactor.tsx:1–60`
- **Lens:** llm
- **Rationale:** Adding the autoFocus prop improves composability and prevents focus conflicts when this component is conditionally rendered alongside other fields.
- **Suggestion:** Consider typing props explicitly (TypeScript interface) to avoid implicit any and make usage clearer.

## Merged duplicates

### MEDIUM · security (duplicate)

- **Location:** `apps/web/components/settings/EnableTwoFactorModal.tsx:200–290`
- **Lens:** llm
- **Rationale:** Copying backup codes to the clipboard (navigator.clipboard.writeText) can expose them to other apps/processes and to clipboard history managers. Additionally, the code does not handle clipboard permission failures, which can lead to a false sense of success if writeText rejects.
- **Merged into:** `llm.enabletwofactormodal.tsx`

### MEDIUM · general (duplicate)

- **Location:** `apps/web/components/settings/DisableTwoFactorModal.tsx:70–130`
- **Lens:** llm
- **Rationale:** Toggling between TOTP and backup code calls resetForm(false), which keeps the password while clearing the factor fields. This is convenient but increases the chance of leaving a sensitive password in form state when switching views, especially if the modal stays open.
- **Merged into:** `llm.disabletwofactormodal.tsx`

### MEDIUM · general (duplicate)

- **Location:** `apps/web/components/settings/EnableTwoFactorModal.tsx:1–320`
- **Lens:** llm
- **Rationale:** The PR introduces a new modal step (DisplayBackupCodes), new UI actions (copy/close), and altered enable flow (onEnable now occurs after closing the backup-codes step). Without tests, regressions in step transitions or failure handling could lock users out or prevent onboarding.
- **Merged into:** `llm.enabletwofactormodal.tsx`

### MEDIUM · general (duplicate)

- **Location:** `apps/web/components/settings/DisableTwoFactorModal.tsx:1–170`
- **Lens:** llm
- **Rationale:** Disable flow now has two paths (TOTP vs backup code) and new error codes (IncorrectBackupCode, MissingBackupCodes). Without tests, error mapping and the lost-access toggle could regress and block disabling 2FA.
- **Merged into:** `llm.disabletwofactormodal.tsx`
