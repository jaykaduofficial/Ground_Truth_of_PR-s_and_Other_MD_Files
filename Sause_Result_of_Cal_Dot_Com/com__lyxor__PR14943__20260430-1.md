# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14943__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14943__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14943__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14943__20260430@pr:1`
- **Files changed:** 3
- **Route:** code_pr_ensemble
- **Reviewed:** 7/15/2026, 5:15:29 PM

## Metrics

- **Findings:** 2 unique (5 raw) · **Files flagged:** 2 · **Density:** 0.7 findings/file
- **Severity:** critical 0 · high 0 · medium 2
- **Files changed:** 3
- **Route:** code_pr_ensemble
- **By category:** general 1 · authz 1
- **Top files:** scheduleSMSReminders.ts (1), migration.sql (1)
- **Sources:** lens 0 · llm 5 · merged 2
- **Duplicates merged:** 3

## Summary

The PR adds a non-null `retryCount` column (with default) to `WorkflowReminder`, which is generally safe but requires the usual downstream steps (e.g., regenerating Prisma client) due to the schema change. In `scheduleSMSReminders.ts`, the updated `deleteMany` filter is overly broad: it will delete any `WorkflowReminder` with `retryCount > 1` regardless of method/type or `scheduledDate`, which risks removing unrelated reminders.

## Findings

### MEDIUM · general

- **Location:** `packages/features/ee/workflows/api/scheduleSMSReminders.ts:28–46`
- **Lens:** llm
- **Rationale:** The deleteMany condition now removes any WorkflowReminder with retryCount > 1 regardless of method/type or scheduledDate. This can unintentionally delete non-SMS reminders (or future-scheduled reminders) if they happen to accrue retryCount, causing data loss and preventing other reminder channels from being processed.
- **Suggestion:** Restrict the retryCount-based deletion to the same scope as the SMS cleanup (e.g., include method: SMS and/or scheduledDate constraints in that branch), or add additional predicates (e.g., only delete failed SMS reminders that are past due).

### MEDIUM · authz

- **Location:** `packages/prisma/migrations/20240508134359_add_retry_count_to_workflow_reminder/migration.sql:1–2`
- **Lens:** llm
- **Rationale:** Adding a new non-null column with a default is generally safe, but it is a schema change that may require regenerating Prisma client and ensuring all reads/writes and selects are compatible. Any raw SQL or strict selects elsewhere could break if they assume the previous column set.
- **Suggestion:** Confirm Prisma client is regenerated and search for any raw SQL/DTOs that enumerate columns for WorkflowReminder; update them as needed or ensure they are resilient to added columns.

## Merged duplicates

### MEDIUM · general (duplicate)

- **Location:** `packages/features/ee/workflows/api/scheduleSMSReminders.ts:163–205`
- **Lens:** llm
- **Rationale:** retryCount is incremented using reminder.retryCount + 1 from a previously read value, which is vulnerable to race conditions if multiple workers/processes handle the same reminder concurrently. This can lead to lost updates and inaccurate retry counts, affecting deletion behavior and retry policies.
- **Merged into:** `llm.schedulesmsreminders.ts`

### MEDIUM · general (duplicate)

- **Location:** `packages/features/ee/workflows/api/scheduleSMSReminders.ts:163–205`
- **Lens:** llm
- **Rationale:** On failure, retryCount is incremented but the code does not appear to adjust scheduledDate or add backoff/jitter. If scheduledDate remains within the selection window, the reminder may be retried immediately on subsequent runs, potentially hammering the SMS provider and increasing costs.
- **Merged into:** `llm.schedulesmsreminders.ts`

### MEDIUM · general (duplicate)

- **Location:** `packages/features/ee/workflows/api/scheduleSMSReminders.ts:1–205`
- **Lens:** llm
- **Rationale:** The retry behavior and deletion criteria are critical control-flow changes, but no tests are shown to validate: increment on provider failure, cap behavior (retryCount > 1), and that only intended reminders are deleted/selected.
- **Merged into:** `llm.schedulesmsreminders.ts`
