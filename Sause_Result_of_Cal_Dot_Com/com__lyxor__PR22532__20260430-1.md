# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22532__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22532__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22532__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22532__20260430@pr:1`
- **Files changed:** 17
- **Route:** code_pr_ensemble
- **Reviewed:** 7/15/2026, 5:29:41 PM

## Metrics

- **Findings:** 6 unique (7 raw) · **Files flagged:** 6 · **Density:** 0.4 findings/file
- **Severity:** critical 0 · high 0 · medium 5 · info 1
- **Files changed:** 17
- **Route:** code_pr_ensemble
- **By category:** general 4 · authz 1 · security 1
- **Top files:** package.json (1), CalendarService.ts (1), CredentialActionsDropdown.tsx (1), calendar-cache.repository.ts (1), getConnectedDestinationCalendars.ts (1)
- **Sources:** lens 0 · llm 7 · merged 6
- **Duplicates merged:** 1

## Summary

Several medium-risk concerns: `updateManyByCredentialId(...,{})` may be a Prisma no-op, `getCacheStatusByCredentialIds` uses `groupBy/_max` with potential edge cases (missing rows, nulls), and the UI exposes “delete cache by credentialId” which must be backed by strict TRPC authorization/ownership checks. There’s also a breaking type change where `UserWithCalendars` now requires additional `SelectedCalendar` fields, plus lower-risk changes including a simplified `findMany` without the `satisfies` check and a `dev:cron` switch from `ts-node` to `tsx` that could affect module resolution/runtime behavior.

## Findings

### MEDIUM · general

- **Location:** `packages/app-store/googlecalendar/lib/CalendarService.ts:1019–1023`
- **Lens:** llm
- **Rationale:** Calling updateManyByCredentialId(..., {}) after availability refresh may be a no-op depending on Prisma behavior; if it doesn't touch updatedAt, the intended "last updated" indicator may never change. If Prisma rejects empty updates in some configurations, this could throw at runtime during a refresh path.
- **Suggestion:** Explicitly update a field (e.g., set updatedAt: new Date()) or use a dedicated method that performs a minimal update guaranteed to bump updatedAt. Add a unit/integration test asserting SelectedCalendar.updatedAt changes after refreshAvailability.

### MEDIUM · authz

- **Location:** `packages/lib/getConnectedDestinationCalendars.ts:18–34`
- **Lens:** llm
- **Rationale:** The exported UserWithCalendars type changes by adding required SelectedCalendar properties (updatedAt, googleChannelId) to the Pick. Downstream code that constructs/mocks UserWithCalendars (tests, other packages) may break or require updates.
- **Suggestion:** Audit usages of UserWithCalendars across the repo and update constructors/mocks accordingly. If this type is used as a public contract, consider making new fields optional unless truly required.

### MEDIUM · security

- **Location:** `packages/features/apps/components/CredentialActionsDropdown.tsx:31–58`
- **Lens:** llm
- **Rationale:** The UI allows deleting cache by credentialId; if the server-side TRPC procedure does not strictly authorize ownership/access to the credential, this becomes an IDOR risk. The client-side checks (integrationType, delegationCredentialId) are not security controls.
- **Suggestion:** Ensure the server-side viewer.calendars.deleteCache procedure validates the credential belongs to the current user (and respects delegation rules) before deleting. Add an authorization test asserting other users cannot delete another user's credential cache.

### MEDIUM · general

- **Location:** `packages/features/calendar-cache/calendar-cache.repository.ts:169–189`
- **Lens:** llm
- **Rationale:** New repository method getCacheStatusByCredentialIds uses groupBy/_max semantics; edge cases (no rows for a credential, null updatedAt, duplicate handling) are easy to get wrong and may affect UI display (e.g., missing entries vs. null).
- **Suggestion:** Add tests covering: (1) multiple cache rows per credential returns latest updatedAt, (2) credentialIds with no cache rows return no entry (or decide/implement expected behavior), and (3) large credentialIds list behavior/performance constraints if relevant.

### MEDIUM · general

- **Location:** `apps/web/package.json:8–15`
- **Lens:** llm
- **Rationale:** Switching dev:cron from ts-node to npx tsx changes runtime/transpilation behavior and can affect ESM/CJS resolution and tsconfig paths. This may surprise contributors if tsx isn't already in dependencies or behaves differently in their environment.
- **Suggestion:** Confirm tsx is declared in the relevant devDependencies and document the change in contributing/dev docs. Consider pinning tsx version (avoid relying on global npx resolution) if reproducibility matters.

### INFO · general

- **Location:** `packages/lib/server/repository/selectedCalendar.ts:257–260`
- **Lens:** llm
- **Rationale:** The findMany implementation was simplified; behavior should be identical but removing the satisfies type check may reduce compile-time assurance when refactoring argument shapes.
- **Suggestion:** Optionally keep the satisfies Prisma.SelectedCalendarFindManyArgs check if it has prevented regressions, or add a small typed helper to preserve the intent without extra noise.

## Merged duplicates

### MEDIUM · security (duplicate)

- **Location:** `packages/features/apps/components/CredentialActionsDropdown.tsx:31–58`
- **Lens:** llm
- **Rationale:** The UI allows deleting cache by credentialId; if the server-side TRPC procedure does not strictly authorize ownership/access to the credential, this becomes an IDOR risk. The client-side checks (integrationType, delegationCredentialId) are not security controls.
- **Merged into:** `llm.credentialactionsdropdown.tsx`
