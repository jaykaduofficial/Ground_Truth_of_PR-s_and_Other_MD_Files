# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22345__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22345__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22345__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR22345__20260430@pr:1`
- **Files changed:** 2
- **Route:** code_pr_ensemble
- **Reviewed:** 7/15/2026, 8:16:35 PM

## Metrics

- **Findings:** 2 unique (6 raw) · **Files flagged:** 2 · **Density:** 1 findings/file
- **Severity:** critical 0 · high 1 · medium 1
- **Files changed:** 2
- **Route:** code_pr_ensemble
- **By category:** authz 1 · general 1
- **Top files:** insightsBooking.integration-test.ts (1), insightsBooking.ts (1)
- **Sources:** lens 0 · llm 6 · merged 2
- **Duplicates merged:** 4

## Summary

High: `getBaseConditions` can return `NOTHING_CONDITION` (`1=0`) when both `authConditions` and `filterConditions` are missing, which may unintentionally force queries to return no rows and potentially break default/unauthenticated flows. Medium: integration tests assert SQL fragments without explicit parentheses around `OR` groups, which can hide operator-precedence issues and make tests pass even if the generated SQL logic is wrong.

## Findings

### HIGH · authz

- **Location:** `packages/lib/server/service/insightsBooking.ts:67–90`
- **Lens:** llm
- **Rationale:** getBaseConditions returns NOTHING_CONDITION (1=0) when both authConditions and filterConditions are null/absent. Depending on intended semantics, this can silently return no rows when no filters are provided (e.g., if buildAuthorizationConditions could ever return null/empty in some scope), differing from previous behavior where AND composition might have allowed broader results. This is a high-impact behavior change if any path yields no conditions.
- **Suggestion:** Audit buildAuthorizationConditions/buildFilterConditions to ensure auth never becomes null/empty when it should allow access; consider returning Prisma.sql`1=1` when both are null if the intended default is 'no additional filtering', or assert/throw when options/authorization cannot be determined.

### MEDIUM · general

- **Location:** `packages/lib/server/service/__tests__/insightsBooking.integration-test.ts:286–372`
- **Lens:** llm
- **Rationale:** Several expected SQL conditions in tests omit explicit parentheses around OR groups in some cases, which can mask operator precedence bugs. SQL AND has higher precedence than OR; if the production SQL builder similarly omits parentheses, results can differ from the prior Prisma AND/OR structure and accidentally broaden access.
- **Suggestion:** Ensure production SQL always groups logic explicitly (wrap OR branches in parentheses) and update tests to assert the fully parenthesized intended structure. Add an integration test that would fail under incorrect precedence (e.g., mixed OR/AND with overlapping team/user bookings).

## Merged duplicates

### HIGH · general (duplicate)

- **Location:** `packages/lib/server/service/insightsBooking.ts:67–90`
- **Lens:** llm
- **Rationale:** getBaseConditions returns NOTHING_CONDITION (1=0) when both authConditions and filterConditions are null/absent. Depending on intended semantics, this can silently return no rows when no filters are provided (e.g., if buildAuthorizationConditions could ever return null/empty in some scope), differing from previous behavior where AND composition might have allowed broader results. This is a high-impact behavior change if any path yields no conditions.
- **Merged into:** `llm.insightsbooking.ts`

### MEDIUM · security (duplicate)

- **Location:** `packages/lib/server/service/insightsBooking.ts:1–120`
- **Lens:** llm
- **Rationale:** Moving from Prisma WhereInput to raw SQL fragments increases the risk of SQL injection and subtle SQL correctness issues (operator precedence, quoting, null semantics). While Prisma.sql with interpolations is generally parameterized, any future string concatenation or Prisma.raw usage could introduce injection vulnerabilities, and callers now execute $queryRaw directly.
- **Merged into:** `llm.insightsbooking.ts`

### MEDIUM · general (duplicate)

- **Location:** `packages/lib/server/service/__tests__/insightsBooking.integration-test.ts:446–520`
- **Lens:** llm
- **Rationale:** Caching tests were removed. Since the service still caches auth and filter conditions, regressions in caching behavior (e.g., recomputation, stale cache across option/filter changes, or mutation of cached Prisma.Sql fragments) will no longer be caught.
- **Merged into:** `llm.insightsbooking.integration-test.ts`

### INFO · general (duplicate)

- **Location:** `packages/lib/server/service/__tests__/insightsBooking.integration-test.ts:1–20`
- **Lens:** llm
- **Rationale:** Tests now compare Prisma.Sql objects directly (toEqual(Prisma.sql`...`)). Depending on Prisma’s internal representation, this can be brittle across Prisma versions or formatting changes, causing false negatives.
- **Merged into:** `llm.insightsbooking.integration-test.ts`
