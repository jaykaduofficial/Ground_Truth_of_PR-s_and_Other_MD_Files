# PR Review: lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14740__20260430 #1

- **PR:** https://github.com/lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14740__20260430/pull/1
- **Base scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14740__20260430@main`
- **PR scope:** `code:lyxor-pr-testing-org/cal_dot_com__cal.com__lyxor__PR14740__20260430@pr:1`
- **Files changed:** 15
- **Route:** code_pr_llm
- **Reviewed:** 7/15/2026, 5:18:12 PM

## Metrics

- **Findings:** 0 · **Files flagged:** 3 · **Density:** 0.6 findings/file
- **Severity:** critical 0 · high 0 · medium 0 · warn 8 · info 1
- **Files changed:** 15
- **Route:** code_pr_llm
- **By category:** logic 4 · test_coverage 2 · security 1 · breaking_api 1 · style 1
- **Top files:** email-manager.ts (4), AddGuestsDialog.tsx (3), BookingListItem.tsx (2)
- **Sources:** lens 0 · llm 0 · merged 0

## Summary

This PR adds UI and backend email notifications to allow adding additional guests to an existing booking from the bookings list. It introduces a new AddGuestsDialog component, new i18n strings, and new organizer/attendee email templates plus a sendAddGuestsEmails helper.

## Findings

### WARN · logic

- **Location:** `apps/web/components/dialog/AddGuestsDialog.tsx:1–107`
- **Rationale:** The dialog initializes MultiEmail with `[""]` and `handleAdd` only checks `multiEmailValue.length === 0`. This allows attempting to validate/mutate with an empty string present, which will fail email validation and can create a confusing UX (error shown even though user hasn't entered anything).
- **Suggestion:** Initialize with `[]` instead of `[""]`, and in `handleAdd` trim/filter empties before validation (e.g., `const emails = multiEmailValue.map(e=>e.trim()).filter(Boolean)`), then validate/mutate using `emails`.

### WARN · logic

- **Location:** `apps/web/components/dialog/AddGuestsDialog.tsx:20–38`
- **Rationale:** The uniqueness refinement checks exact string equality, but does not normalize case/whitespace. Emails like `Test@a.com` and `test@a.com` or `a@b.com ` can bypass uniqueness or fail unexpectedly depending on downstream behavior.
- **Suggestion:** Normalize before validation (trim + lowercase) and run uniqueness on normalized values; also pass normalized emails to the mutation.

### WARN · logic

- **Location:** `packages/emails/email-manager.ts:522–553`
- **Rationale:** In `sendAddGuestsEmails`, the attendee email logic appears inverted: if `newGuests.includes(attendee.email)` it sends `AttendeeScheduledEmail`, else it sends `AttendeeAddGuestsEmail`. This would email newly added guests the standard scheduled template while sending existing attendees the 'guests added' notification, which is likely the opposite of intended behavior.
- **Suggestion:** Swap the branches: send `AttendeeAddGuestsEmail` to existing attendees, and send `AttendeeScheduledEmail` (or a dedicated 'you were added' template) to the newly added guests.

### WARN · logic

- **Location:** `packages/emails/email-manager.ts:522–553`
- **Rationale:** Organizer emails are always sent to organizer plus every team member when `calendarEvent.team?.members` exists, regardless of whether team members should be notified for this booking (and potential duplication if organizer is also in members). This can cause noisy notifications and duplicate emails.
- **Suggestion:** De-duplicate recipients (by email) and ensure only relevant team members are included (e.g., those assigned/hosts for the booking), or gate with a config/role check.

### WARN · security

- **Location:** `apps/web/components/booking/BookingListItem.tsx:189–356`
- **Rationale:** Adding guests changes booking attendees, which is a sensitive action. The UI exposes the action broadly; without confirming server-side authorization on `viewer.bookings.addGuests`, users might add guests to bookings they should only view (e.g., team bookings).
- **Suggestion:** Ensure the TRPC `viewer.bookings.addGuests` procedure enforces authorization (booking ownership/host/team permissions) and rate limits; add tests verifying unauthorized users cannot add guests.

### WARN · test_coverage

- **Location:** `apps/web/components/dialog/AddGuestsDialog.tsx:1–107`
- **Rationale:** New dialog behavior (validation, uniqueness, submit/cancel, mutation success/error) is untested. This is a user-facing feature and a common source of regressions.
- **Suggestion:** Add component tests (e.g., React Testing Library) covering: cannot submit empty input, invalid email shows error, duplicate emails show error, successful mutation closes dialog and invalidates bookings, error toast shown on mutation failure.

### WARN · test_coverage

- **Location:** `packages/emails/email-manager.ts:522–553`
- **Rationale:** The new email routing logic for add-guests is prone to subtle mistakes (as evidenced by the likely inversion). Without unit tests, it's easy to send incorrect templates to recipients.
- **Suggestion:** Add unit tests for `sendAddGuestsEmails` verifying which template is sent to organizer, team members, existing attendees, and newly added guests, including de-duplication and correct subject/title keys.

### WARN · breaking_api

- **Location:** `packages/emails/email-manager.ts:1–553`
- **Rationale:** A new exported function `sendAddGuestsEmails` is introduced. While additive, if other parts of the codebase are expected to call it, the PR should ensure it's wired into the actual add-guests flow; otherwise the feature may appear to work in UI but not send notifications.
- **Suggestion:** Confirm the backend mutation invokes `sendAddGuestsEmails(calEvent, newGuests)` after successfully persisting guests, and add an integration test asserting emails are queued/sent when guests are added.

### INFO · style

- **Location:** `apps/web/components/booking/BookingListItem.tsx:189–210`
- **Rationale:** The action id/label use `add_members` / `additional_guests`, mixing 'members' vs 'guests'. This can reduce clarity and makes test selectors less descriptive.
- **Suggestion:** Rename ids/testids consistently (e.g., `add_guests`) to match the feature terminology.
